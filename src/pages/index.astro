---
import BaseLayout from '../layouts/BaseLayout.astro';

const email = Astro.url.searchParams.get('email') || '';
---

<BaseLayout title={email ? `${email} — Hercules CRM` : 'Hercules CRM'}>
  <header class="bg-white border-b border-gray-200 px-6 py-4 flex items-center justify-between">
    <div class="flex items-center gap-3">
      <div class="w-8 h-8 bg-[#c8102e] rounded-lg flex items-center justify-center text-white font-bold text-sm">H</div>
      <h1 class="text-lg font-semibold">Hercules CRM</h1>
    </div>
    {email && (
      <div class="text-sm text-gray-500">{email}</div>
    )}
  </header>

  <main class="max-w-5xl mx-auto px-6 py-8">
    {email ? (
      <div>
        <p class="text-gray-500 text-sm mb-2">Customer lookup</p>
        <div class="bg-white rounded-xl border border-gray-200 p-6">
          <p class="text-lg font-medium">{email}</p>
          <p class="text-gray-400 mt-2">Customer identification coming next...</p>
        </div>
      </div>
    ) : (
      <div class="text-center py-20">
        <div class="w-16 h-16 bg-gray-100 rounded-2xl flex items-center justify-center mx-auto mb-4">
          <svg xmlns="http://www.w3.org/2000/svg" class="w-8 h-8 text-gray-400" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M21.75 6.75v10.5a2.25 2.25 0 01-2.25 2.25h-15a2.25 2.25 0 01-2.25-2.25V6.75m19.5 0A2.25 2.25 0 0019.5 4.5h-15a2.25 2.25 0 00-2.25 2.25m19.5 0v.243a2.25 2.25 0 01-1.07 1.916l-7.5 4.615a2.25 2.25 0 01-2.36 0L3.32 8.91a2.25 2.25 0 01-1.07-1.916V6.75" />
          </svg>
        </div>
        <h2 class="text-xl font-semibold mb-2">No customer selected</h2>
        <p class="text-gray-500">Open this dashboard from the Missive sidebar to identify a customer.</p>
      </div>
    )}
  </main>
</BaseLayout>
